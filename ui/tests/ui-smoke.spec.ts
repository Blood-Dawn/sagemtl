import { test } from '@playwright/test';

test.describe.skip('SageMTL dashboard shell', () => {
  test('placeholder smoke test', async () => {
    // Placeholder for future Playwright smoke test once API is wired.
  });
});
